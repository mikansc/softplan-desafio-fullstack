import React from "react";
import StyledLoginSection from "./LoginContainer.style";

const LoginContainer = ({ children }) => (
  <StyledLoginSection>{children}</StyledLoginSection>
);

export default LoginContainer;
